# Dev: biarkan KOSONG → axios pakai baseURL '/api' → diteruskan Vite proxy ke localhost:8000
# (lihat vite.config.js). Sesuai intent STEP 12 (fallback '/api' untuk dev).
# Production: di-set di Vercel Project Settings (origin Railway), BUKAN di file ini.
VITE_BACKEND_URL=
